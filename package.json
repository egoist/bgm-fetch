{
  "name": "bgm-fetch",
  "version": "0.0.4",
  "description": "本库用于获取番组更新，返回一个 Promise 给米娜桑。",
  "main": "index.js",
  "scripts": {
    "test": "npm run babel && mocha --compilers js:babel-core/register --require should --require should-promised test.js",
    "babel": "babel src.js --out-file index.js",
    "beforePublish": "npm run babel && npm publish"
  },
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git@github.com:egoist/bgm-fetch.git"
  },
  "dependencies": {
    "isomorphic-fetch": "^2.1.1"
  },
  "devDependencies": {
    "mocha": "^2.3.3",
    "should": "^7.1.0",
    "should-promised": "^0.3.1"
  }
}
